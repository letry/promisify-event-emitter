{
    "defaultEmitterMethodNames": {
        "on": ["addEventListener", "addListener", "on"],
        "off": ["removeEventListener", "removeListener", "off"],
        "emit": ["dispatchEvent", "emit"]
    }
}